.DS_Store
_site
.sass-cache
.jekyll-cache
.jekyll-metadata
.bundles_cache
Gemfile.lock
vendor

# Claude
.claude/
.claude-work/
CLAUDE.md

# SEO toolkit (local-only: GSC credentials + analytics — never commit)
seo/
